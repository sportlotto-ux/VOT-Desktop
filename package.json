{
  "name": "vot-desktop",
  "private": true,
  "version": "0.2.3",
  "type": "module",
  "description": "Desktop app for downloading YouTube videos with Yandex VOT voice translation",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "cargo test --locked --manifest-path src-tauri/Cargo.toml",
    "lint:rust": "cargo fmt --check --manifest-path src-tauri/Cargo.toml && cargo clippy --locked --manifest-path src-tauri/Cargo.toml -- -D warnings",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.1.1",
    "@tauri-apps/plugin-dialog": "^2.0.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0",
    "esbuild": "^0.28.0",
    "typescript": "^7.0.2",
    "vite": "^8.2.1"
  }
}
